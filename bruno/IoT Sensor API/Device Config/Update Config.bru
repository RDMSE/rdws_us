meta {
  name: Update Config
  type: http
  seq: 3
}

put {
  url: {{baseUrl}}/devices/:devId/config
  body: json
  auth: bearer
}

params:path {
  devId: 3
}

auth:bearer {
  token: {{jwtToken}}
}

body:json {
  {
    "config": {
        "report_interval_s": 120,
        "sampling_interval_s": 66
      }
  }
}
