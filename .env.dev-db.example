# Template para .env.dev-db (não versionado) — copiar e preencher no homelab.
# Usado por: docker compose -f docker-compose.dev-db.yml --env-file .env.dev-db up -d
# Valores abaixo espelham .env.dev (mesmo banco, agora containerizado).

DB_USER=rdws_user
DB_PASSWORD=password
DB_NAME=rdws_us_dev
